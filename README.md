step 1 : run command to set virtual envirnment ( .\.venv\Scripts\Activate.ps1 )
step 2 : run fastapi(main.py)  ( python -m uvicorn main:app --reload )
for api check run this on browser http://127.0.0.1:8000/docs#/
step 3 : run frontend file(app.py) ( python -m streamlit run app.py )
to run application http://localhost:8501/

